import React from 'react';
import Button from '@material-ui/core/Box';

const RecentUploadedFiles = () => {
  return (
    <div>
      <Button variant="contained">Recent Uploaded Files</Button>
    </div>
  );
};

export default RecentUploadedFiles;
