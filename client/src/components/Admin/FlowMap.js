import React from 'react';
import Button from '@material-ui/core/Box';

const FlowMap = () => {
  return (
    <div>
      <Button variant="contained">Flow Map</Button>
    </div>
  );
};

export default FlowMap;
