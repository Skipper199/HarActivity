import React from 'react';
import Button from '@material-ui/core/Button';

const HTTPHeaders = () => {
  return (
    <div>
      <Button variant="contained">HTTP Headers</Button>
    </div>
  );
};

export default HTTPHeaders;
