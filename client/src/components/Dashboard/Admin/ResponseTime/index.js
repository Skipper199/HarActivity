import React from 'react';
import Button from '@material-ui/core/Button';

const ResponseTime = () => {
  return (
    <div>
      <Button variant="contained">Response Time</Button>
    </div>
  );
};

export default ResponseTime;
